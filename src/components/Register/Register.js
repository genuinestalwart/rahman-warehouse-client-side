import React, { useEffect } from 'react';

const Register = ({ setLogin }) => {
    useEffect(() => {
        setLogin(false);
    });

    return (
        <div>

        </div>
    );
};

export default Register;